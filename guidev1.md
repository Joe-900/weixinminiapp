---

# 校园阅读伴读小程序 · 完整架构与开发指南

## 0. 你的角色与工作方式

你是本项目的高级架构师兼全栈开发助手。请严格按本指南设计与编码。

* 严格遵守约定的技术栈、目录结构、数据模型、接口契约,不得擅自更换框架或引入未约定的重型依赖。
* 全程 TypeScript,开启 strict,禁止用 any 兜底类型。
* 所有数据写操作和外部 API 调用必须在云函数完成。前端不得持有任何密钥,不得直接写数据库。
* 每完成一个模块,必须给出该模块的自测说明,对照第 11 节验收项。
* 遇到不确定的微信云开发 API 或大模型接口细节,显式写明"需按官方最新文档核实",不要编造接口或参数。
* 输出代码时给出文件路径,小步、可读、关键逻辑加注释,方便人工 review。
* 如本指南与你的默认习惯冲突,以本指南为准。

## 1. 项目背景与硬约束

项目定位:面向中学的校园阅读伴读小程序。核心功能为精选书单、书籍详情、AI 阅读伴读、读书笔记与打卡。开发以 AI 生成代码为主,团队同学做 code review。

硬约束(违反任意一条视为设计错误):

1. 不对接图书馆任何真实数据库或内部系统。合规要求,无协商空间。
2. 书籍只存元数据(书名、作者、ISBN、简介、封面),不存任何书籍全文,规避版权。
3. 数据只来自两处:管理员人工录入、可选的公开图书 API 辅助填充。运行时用户访问不依赖任何外部数据源。
4. 权限校验必须在后端云函数完成。前端隐藏只是体验优化,不得作为安全手段。
5. 大模型 key 与 baseURL 只存在云函数环境变量,任何前端代码、仓库、网络响应中都不得出现明文 key。

## 2. 技术栈(锁定,不可更改)

* 前端:Taro 最新稳定版 + React + TypeScript
* UI 组件:优先轻量、与 Taro 兼容的方案,避免引入不兼容库
* 状态管理:React Context 或 Zustand,禁止 Redux 全家桶
* 后端:微信云开发 CloudBase(云函数 + 云数据库 + 云存储)
* AI 接入:OpenAI 兼容格式(baseURL + key),默认对接 DeepSeek
* 质量工具:ESLint + Prettier + TypeScript strict

## 3. 整体架构(分层职责)

系统分五层,数据自上而下流动,职责严格分离:

* 数据源层:公开图书 API(仅录入时辅助填充)、管理员人工录入。数据进入系统的唯一来源。
* 前端层(Taro):只负责展示和交互。包含页面、状态层、服务层。服务层是调用云函数的唯一出口。
* 云函数调度层:系统唯一的写入口和外部调用出口。每个请求先鉴权,再分发到业务域。
* 数据层:云数据库存自有业务数据,云存储存封面图。
* 外部大模型层:仅 AI 业务域可调用,走 OpenAI 兼容格式。

核心数据流向:用户在前端操作,经服务层发请求到云函数;云函数鉴权后读写云数据库或调用大模型,再返回前端。书籍数据只在管理员录入时写入,普通用户对书籍只读。运行时不访问任何外部数据源。

设计铁律:前端只读展示加发起请求;云函数是唯一写入口和外部出口;数据库只存自有数据。

## 4. 全局技术契约(模型必须统一遵守)

### 4.1 登录态与身份

* 使用微信云开发能力。用户身份 openid 由云函数运行时上下文获取(在云函数内通过 cloud 提供的上下文取 OPENID),前端不传 openid,后端也绝不信任前端传来的 openid。
* 前端首次启动调用 user.login,云函数据上下文 openid 查 user 集合:不存在则创建一条 role 为 user 的记录,存在则返回。返回 openid 与 role 给前端缓存。
* 任何涉及"本人数据"或"角色"的判断,一律以云函数上下文 openid 和数据库 role 为准。

### 4.2 统一返回结构

所有云函数返回统一信封,前端服务层据此判断:

```
{
  code: number,      // 0 表示成功,非 0 表示错误
  message: string,   // 给前端展示或调试的提示
  data: any | null   // 成功时的业务数据
}
```

### 4.3 统一错误码

| code | 含义       | 典型场景                 |
| ---- | -------- | -------------------- |
| 0    | 成功       | 正常返回                 |
| 1001 | 未登录或身份无效 | 无法获取 openid          |
| 1002 | 权限不足     | 非 admin 调用管理操作       |
| 1003 | 入参缺失或非法  | 必填字段为空、类型错误          |
| 1004 | 资源不存在    | bookId/sessionId 查不到 |
| 1005 | 越权访问     | 读取非本人数据              |
| 2001 | 大模型调用失败  | 接口超时、报错              |
| 2002 | 触发用量限制   | 超出单日次数或单次长度          |
| 5000 | 服务器内部错误  | 未预期异常兜底              |

### 4.4 入参校验

每个 action 进入业务逻辑前先校验入参,缺失或类型错误返回 1003 并说明哪个字段,不得让云函数抛未捕获异常。

## 5. 前端结构与约定

目录结构:

```
src/
  pages/
    home/         书单首页
    bookDetail/   书籍详情
    aiChat/       AI伴读对话
    notes/        我的笔记打卡
    profile/      个人中心
    admin/        管理员书籍管理(按角色条件进入)
  components/
    BookCard/     书籍卡片
    ChatBubble/   聊天气泡
    NoteItem/     笔记条目
    StateView/    空态与加载
    AuthGuard/    权限包裹组件
  store/          状态层
  services/       云函数调用封装(唯一收口)
  types/          前后端共享类型定义
  utils/          工具函数
  app.config.ts   路由与tab配置
```

约定:

* 页面禁止直接调用云函数,必须经 services 层。统一在此处理 loading、错误码到提示的映射、重试。
* services 层每个方法对应一个 action,入参出参用 types 中的类型标注。
* types 目录与云函数共享同一套数据模型类型,字段必须一致,防止前后端漂移。
* 底部 tab 三个:书单首页、AI 伴读入口、个人中心。管理员页不进 tab,由个人中心按 role 条件进入,用 AuthGuard 包裹。
* 状态层只存必要状态:用户与角色、书籍列表缓存、当前会话上下文。
* 所有列表页实现下拉刷新与上拉分页加载,统一用 StateView 处理空态、加载中、加载失败。

## 6. 后端云函数设计

按业务域聚合,每域一个云函数,内部用 event.action 分发。入口统一先鉴权(取上下文 openid,查 role),再分发;写操作 action 二次校验角色。

业务域:user、book、note、ai。

接口契约(action 路由表):

| 域    | action       | 入参                                  | 出参 data          | 权限      |
| ---- | ------------ | ----------------------------------- | ---------------- | ------- |
| user | login        | 无                                   | openid, role     | 公开      |
| user | profile      | nickname, avatar                    | user             | 登录      |
| book | list         | page, pageSize, keyword?            | book 列表, total   | 登录      |
| book | detail       | bookId                              | book             | 登录      |
| book | create       | title, author, isbn, summary, cover | bookId           | 仅 admin |
| book | update       | bookId, 可选字段                        | ok               | 仅 admin |
| book | offline      | bookId                              | ok               | 仅 admin |
| book | online       | bookId                              | ok               | 仅 admin |
| note | addNote      | bookId, content                     | noteId           | 登录      |
| note | listNote     | page, pageSize                      | note 列表, total   | 登录,仅本人  |
| note | deleteNote   | noteId                              | ok               | 登录,仅本人  |
| note | checkIn      | bookId, minutes                     | ok               | 登录      |
| note | checkInStat  | 无                                   | 连续打卡天数, 累计时长     | 登录,仅本人  |
| ai   | chat         | sessionId?, bookId, question        | reply, sessionId | 登录      |
| ai   | loadHistory  | sessionId                           | message 列表       | 登录,仅本人  |
| ai   | listSessions | 无                                   | session 列表       | 登录,仅本人  |

实现规则:

* create、update、offline、online 在鉴权后必须再查 role,非 admin 返回 1002。
* listNote、loadHistory、listSessions、deleteNote 只能操作当前 openid 自己的数据,越权返回 1005。
* list 只返回 status 为 online 的书籍;管理员页另用带 status 过滤的查询展示全部。
* 分页统一用 page 从 1 起、pageSize 默认 20,返回总数 total 供前端分页。

## 7. 数据库设计

使用云数据库,共六个集合。书籍集合严格只存元数据,无全文。

user 集合:openid(主键,登录态来源)、nickname、avatar、role(user 或 admin,默认 user)、createdAt。

book 集合:bookId(主键)、title、author、isbn、cover(云存储 fileID,不存外链)、summary、status(online 或 offline,下架只改状态不删)、addedBy(录入管理员 openid)、createdAt、updatedAt。

note 集合:noteId(主键)、openid(所属用户)、bookId、content、createdAt。

checkin 集合:checkinId(主键)、openid、bookId、minutes、checkinDate。

ai_session 集合:sessionId(主键)、openid、bookId、title(可取首条提问摘要)、createdAt。

ai_message 集合:msgId(主键)、sessionId(所属会话)、role(user 或 assistant,对应 OpenAI 消息结构)、content、createdAt。

关系:一个用户有多条笔记、多条打卡、多个会话;一本书关联多条笔记、多个会话;一个会话包含多条消息。

索引建议:book 按 status 加 createdAt;note 与 ai_session 按 openid;checkin 按 openid 加 checkinDate;ai_message 按 sessionId 加 createdAt。

权限规则:云数据库集合权限设为"仅创建者可读写"或"所有人不可写"。所有写入一律走云函数,禁止开放前端直接写权限。

## 8. 管理员引导(第一个 admin 怎么来)

* 不做管理员注册流程。第一个管理员由开发者在云开发控制台手动操作:在 user 集合找到该同学的记录,把 role 字段改为 admin。
* 前端管理入口仅作展示控制:role 为 admin 才显示。真正的权限拦截在云函数 role 校验,前端展示控制不可替代。
* 若后续需要多个管理员,由现有 admin 通过受保护的接口提升(本期不做,留扩展位)。

## 9. 封面图上传链路

1. 管理员在书籍管理页选择本地图片。
2. 前端调用云存储上传接口,拿到 fileID。
3. 调用 book.create 或 book.update,把 fileID 作为 cover 字段传入。
4. 用户端展示时用 fileID 渲染图片(云开发可直接用 fileID 显示)。
5. 禁止把外链图片 URL 当封面入库,外链易失效且有外域限制。

## 10. AI 伴读模块

### 10.1 调用流程

1. 前端发起 ai.chat,传 bookId、question,续聊时带 sessionId。
2. 云函数取上下文 openid 校验登录;无 sessionId 则新建会话。
3. 读取该会话历史消息(按时间升序,限制最近 N 条,建议 N 为 10)。
4. 读取该 bookId 的元数据(标题、作者、简介)。
5. 拼装 messages:system 提示 + 书籍上下文 + 历史消息 + 本次提问。
6. 以 OpenAI 兼容格式请求大模型 chat completions。
7. 把用户消息和 AI 回复写入 ai_message。
8. 返回 reply 与 sessionId。

### 10.2 system prompt 模板(模型按此实现,变量运行时填入)

```
你是校园阅读伴读助手,服务于中学生读者。
当前书籍信息如下:
书名:{title}
作者:{author}
简介:{summary}
你的任务是基于上述书目信息,引导学生思考、激发阅读兴趣、解答与本书相关的阅读疑问、对学生的读后感给予鼓励性点评。
重要限制:你没有读过本书全文,只掌握以上书目信息。不要编造书中具体情节、原文引文或细节。当被问及具体情节时,坦诚说明并引导学生自己去书中寻找,或从主题层面展开讨论。
回答风格:友善、鼓励、贴近中学生认知水平,避免说教。
```

### 10.3 用量保护

* 单用户单日 chat 调用次数上限(建议默认 50,设为可配置)。
* 单次 question 长度上限(建议默认 1000 字)。
* 拼装的历史消息条数上限(建议 10 条),超出只取最近的。
* 触发限制返回 2002 与友好提示。

### 10.4 异常兜底

* 大模型超时或报错返回 2001,前端展示降级提示(如"伴读助手有点忙,稍后再试"),不得卡死或白屏。
* 大模型调用要设置合理超时时间,避免云函数长时间挂起。

### 10.5 模型可替换

* baseURL、key、model 名称全部从云函数环境变量读取。
* 切换模型只需改环境变量,不得改动业务代码。

## 11. 测试与验收要求

模型交付每个模块时附自测说明;整体交付需通过下列验收项。

### 11.1 功能验收

| 编号 | 验收项     | 通过标准                                |
| -- | ------- | ----------------------------------- |
| F1 | 静默登录    | 首次进入自动登录,返回 openid 与 role           |
| F2 | 书单展示    | 首页分页展示 online 书籍,封面正常加载,支持下拉刷新与上拉加载 |
| F3 | 书籍详情    | 进入详情显示完整元数据                         |
| F4 | AI 对话   | 提问返回围绕该书的合理回复,多轮上下文连贯               |
| F5 | 对话历史    | 重进会话能加载历史消息;会话列表可查看                 |
| F6 | 写笔记     | 笔记保存并出现在"我的笔记",可删除                  |
| F7 | 打卡      | 打卡写入成功,可查看连续天数与累计时长                 |
| F8 | 管理员上架   | admin 能新增书籍并上传封面,普通用户无入口            |
| F9 | 管理员下架上架 | 下架后用户端列表不显示,数据未删除,可重新上架             |

### 11.2 权限与安全验收(重点)

| 编号 | 验收项    | 通过标准                                        |
| -- | ------ | ------------------------------------------- |
| S1 | 后端权限兜底 | 用普通用户身份直接调用 create/offline/update,后端返回 1002 |
| S2 | 数据隔离   | 用户只能读到自己的笔记、打卡、会话,读他人数据返回 1005              |
| S3 | 密钥安全   | 前端代码与网络请求中不出现大模型 key                        |
| S4 | 集合权限   | 云数据库集合未开放前端直接写权限                            |
| S5 | 入参校验   | 非法或缺失入参返回 1003 并指明字段,云函数不崩溃                 |
| S6 | 身份不可伪造 | 前端伪造 openid 不影响后端,后端只认上下文 openid            |

S1 与 S6 为必测项:必须实际模拟"绕过前端、直接构造请求"的场景验证后端拒绝,不能只靠前端隐藏按钮判定通过。

### 11.3 AI 专项验收

| 编号 | 验收项   | 通过标准                      |
| -- | ----- | ------------------------- |
| A1 | 上下文注入 | 回复体现对应书籍信息,而非泛泛而谈         |
| A2 | 不编造情节 | 追问具体情节时坦诚说明未读全文,不杜撰       |
| A3 | 模型可替换 | 仅改环境变量即可切换模型,业务代码不变       |
| A4 | 用量保护  | 超单日次数或单次长度时返回 2002 与友好提示  |
| A5 | 异常兜底  | 接口超时或报错时返回 2001,前端降级提示不卡死 |

### 11.4 代码质量验收

| 编号 | 验收项    | 通过标准                   |
| -- | ------ | ---------------------- |
| Q1 | 类型检查   | tsc 无报错,无 any 滥用       |
| Q2 | 静态检查   | ESLint 无 error 级问题     |
| Q3 | 调用收口   | 页面无直接调用云函数,均走 services |
| Q4 | 类型一致   | 前后端共享数据模型类型一致          |
| Q5 | 返回结构统一 | 所有云函数遵守统一信封与错误码        |

### 11.5 验收方式

* 功能项:在微信开发者工具用开发版逐项手动走查,记录结果。
* 权限项:用两个测试账号(一个手动设为 admin,一个普通)对照测试,并对写操作做绕过前端的接口直连测试。
* AI 项:准备 3 到 5 组测试提问,覆盖正常、追问情节、超长、超频、接口异常场景。
* 代码项:运行类型检查与 lint,人工抽查服务层收口与返回结构一致性。

## 12. 环境与配置

* 云函数环境变量需配置:大模型 baseURL、key、model 名称、单日次数上限、单次长度上限。
* 提供一份环境变量清单文档(只列字段名与用途,不含真实值)。
* 提供云开发环境初始化说明:需创建的集合、各集合权限设置、需建的索引。
* 真实 key、AppID、环境 ID 不写入代码与文档,运行时由配置注入。

## 13. 交付物清单

* 可运行的 Taro 小程序项目(开发版可在开发者工具运行)。
* 全部云函数代码及部署说明。
* 云数据库集合结构、权限配置、索引说明。
* 环境变量清单(不含真实值)。
* 自测报告:对照第 11 节逐项填写结果。

## 14. 实施顺序(建议分阶段交付,每阶段附对应验收)

1. Taro 骨架 + 云开发初始化 + 全局契约(返回信封、错误码、类型基座)+ 登录。对应 F1、S6。
2. 数据库集合与权限 + 索引 + 书籍域读接口 + 书单和详情页。对应 F2、F3、S4。
3. 管理员书籍管理 + 封面上传 + 上下架 + 权限兜底。对应 F8、F9、S1。
4. AI 伴读域 + 对话页 + 会话历史。对应 F4、F5、A1 到 A5。
5. 笔记打卡域 + 相关页面。对应 F6、F7、S2。
6. 工程规范收尾 + 全量验收。对应 Q1 到 Q5 与完整走查。

验收细节说明：

明白。这一节就是要追加进指南的确定文本,而且要贯彻一条新原则:本地模式下一切都不出本机,包括 AI——大模型调用也要能被一个本地假实现替换掉,不连真实接口、不烧 key,照样能把"AI 链路的逻辑"测通。

下面是可直接追加进指南的完整文本块(放在第 11A 节之前,作为更前置的一关)。我把 AI 的本地化单独立了 11B.6,这是这次新增的重点。



## 11B. 本地 Mock 验证层(完全免上云的功能性测试)

为在不连接任何云资源、不调用任何外部接口的前提下,验证模块通信、前后端映射、数据读写、权限逻辑与 AI 链路,项目必须内置本地 Mock 层。本层是第一道功能性验证关,必须在上云前通过。

本节的总原则:在本地模式下,系统的全部依赖(云数据库、云存储、大模型)都由本地假实现替换,整套业务逻辑能在纯 Node/前端模拟器中跑通,不产生任何网络请求,不消耗任何云额度或大模型费用。

### 11B.1 设计要求

1. 业务逻辑与外部依赖解耦。云函数入口只负责:取上下文 openid、获取数据库实例、获取 AI 客户端,然后把这些作为参数传给纯业务函数。业务函数通过抽象接口操作数据与 AI,不得直接 import 或调用云数据库 SDK、大模型 SDK。
2. 三类外部依赖各提供两套实现,接口完全一致、可互换注入:

   * 数据访问(Repository):线上实现基于云数据库;本地实现基于内存。
   * 文件存储(Storage):线上实现基于云存储;本地实现返回模拟 fileID,不真正上传。
   * AI 客户端(AIClient):线上实现走 OpenAI 兼容接口;本地实现为离线假回复(见 11B.6)。
3. 前端服务层支持模式开关。本地模式下,服务层不调用真实云函数,转而调用本地 mock 入口(在内存中直接跑对应 action 的业务函数),返回相同的统一信封。
4. 模式由环境变量或配置常量控制:local 模式用全本地假实现;cloud 模式对接真实云资源。切换不改任何业务代码。

### 11B.2 本地 Mock 层必须覆盖的验证

| 类别     | 验证内容                                      | 方式                |
| ------ | ----------------------------------------- | ----------------- |
| 通信映射   | 前端 service 调用正确路由到对应 action 并拿到信封返回       | Jest 或本地脚本        |
| 入参校验   | 缺字段、错类型入参返回 1003 并指明字段                    | Jest              |
| 权限逻辑   | 普通用户调 create/offline/update 返回 1002       | Jest 注入 role=user |
| 数据隔离   | 用户读非本人 note/session 返回 1005               | Jest 注入不同 openid  |
| 入库逻辑   | create 后内存库出现该记录,字段正确                     | Jest 断言内存库状态      |
| 状态变更   | offline 后该书 status 变 offline 且记录仍在        | Jest 断言未删除        |
| 列表过滤   | list 只返回 status 为 online 的书               | Jest              |
| 分页     | page/pageSize 返回正确条数与 total               | Jest              |
| 封面上传   | 本地 Storage 返回模拟 fileID,create 能正常存入 cover | Jest              |
| 身份不可伪造 | 业务函数只用注入的 openid,入参里的假 openid 被忽略         | Jest              |
| AI 链路  | 见 11B.6 全部条目                              | Jest              |

### 11B.3 内存数据库实现要求

* 提供与线上 Repository 相同方法签名的内存实现(增、删、改、按条件查、分页)。
* 每个测试用例前可重置内存数据,保证用例独立。
* 可预置种子数据(测试用书籍、用户、笔记、会话),供用例使用。

### 11B.4 本地存储实现要求

* 本地 Storage 的上传方法不做真实 IO,直接返回一个固定格式的模拟 fileID(如 local-mock://cover/{随机串})。
* 业务逻辑只关心拿到 fileID 并存库,不关心 fileID 真假,从而无需真实云存储即可验证上传到入库的链路。

### 11B.5 前端本地模式要求

* 本地模式下,模拟器中能用 mock 数据走通主要页面与交互:书单、详情、AI 对话、笔记打卡、管理员管理页。
* 前端不感知当前是本地还是云模式,只通过 service 层拿信封数据。模式切换对页面透明。

### 11B.6 AI 的本地化(离线假实现,重点)

本地模式下严禁调用任何真实大模型接口。AI 能力由 AIClient 的本地假实现提供,目标是把"AI 链路的逻辑"测通,而非验证模型的真实回答质量。

本地 AIClient 要求:

1. 不发起任何网络请求,同步或近似同步返回一个确定的、可预测的假回复。
2. 假回复内容应回显关键上下文,以便断言上下文确实被正确拼装传入。例如回复中包含传入的书名,或回显本次提问,形如:已收到关于《{书名}》的问题:{提问},这是本地模拟回复。
3. 接受与线上实现相同的入参(拼好的 messages 数组),使被测的业务逻辑完全一致。

AI 链路在本地必须验证的条目:

| 编号    | 验证内容          | 断言方式                                              |
| ----- | ------------- | ------------------------------------------------- |
| LAI1  | 新会话创建         | 无 sessionId 调 chat,内存库新建 ai_session,返回新 sessionId |
| LAI2  | 消息落库          | chat 后内存库新增两条 ai_message(user 与 assistant)        |
| LAI3  | 上下文拼装顺序       | 拼出的 messages 为 system 在首、历史按时间、提问在末               |
| LAI4  | system 注入书籍信息 | system 内容包含该 bookId 的书名与简介                        |
| LAI5  | 历史条数上限        | 历史超过上限时只取最近 N 条                                   |
| LAI6  | 续聊            | 带已有 sessionId 调 chat,历史正确累加,不新建会话                 |
| LAI7  | 超长限额          | 提问超长返回 2002,不进入假模型调用                              |
| LAI8  | 超频限额          | 超单日次数返回 2002                                      |
| LAI9  | 异常兜底          | 让本地 AIClient 抛错一次,业务返回 2001,前端可降级                 |
| LAI10 | 假回复落库         | 假回复内容正确写入 assistant 消息                            |

要点:LAI3 与 LAI4 是核心。它们证明了"把 system prompt、书籍上下文、历史、提问拼成正确的 messages"这段最容易出错的逻辑是对的。这段逻辑对错与真实模型无关,因此完全可以、也应该在本地用假模型验证。上云后只需再确认真实接口连通(见第 11A 节 A 类项),不必在本地烧 key 反复试。

### 11B.7 交付物

* 三套抽象接口定义(Repository、Storage、AIClient),各自的云实现与本地实现。
* 业务函数与云入口分离的代码结构(依赖注入)。
* Jest 测试集,覆盖 11B.2 与 11B.6 全部条目,可通过 npm 脚本一键运行。
* 一份本地验证报告:逐条记录 11B.2 与 11B.6 的运行结果。
* 模式切换说明:如何在 local 与 cloud 间切换。

### 11B.8 验证执行顺序(整体两层)

第一层,本地全离线(本节):

1. 切到 local 模式。
2. 跑 Jest,验证 11B.2 与 11B.6 全部逻辑(数据、权限、通信、入库、AI 拼装),全程不联网、不碰云、不调真实模型。
3. 跑 tsc 与 ESLint。
4. 模拟器本地模式走查主要页面交互。
5. 此层不全过,不进入上云。

第二层,上云联调(见第 11A 节):

6. 切到 cloud 模式,验证真实云数据库、云存储、真实大模型接口的连通性。
7. Console 直连测后端权限兜底(S1、S6)。
8. 真机复验关键路径。

通过第一层后,模块通信、前后端映射、入库、权限、AI 拼装逻辑均已在纯本机确认正确;上云只需关注真实连通性,大幅减少反复上云与消耗大模型额度的成本。



一个落地提醒:这套能不能成,关键在 11B.1 的解耦要求——业务逻辑不能和云 SDK、大模型 SDK 缠在一起。建议你喂给 GLM 时把这条单独强调,因为它默认习惯是直接写 `db.collection` 和直接调模型接口,一旦写死就拆不开了。
---
